name: Deploy and Run Migrations

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v3
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Run Migrations
        env:
          DATABASE_URL: "postgresql://budget_api_lthg_user:X6rB3gDQvJUzxjLU5OKP51sB45568XSl@dpg-cv6rv5t2ng1s7380hhag-a/budget_api_lthg"
        run: |
          python manage.py migrate
